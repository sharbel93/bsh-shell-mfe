# mail remote
Port 4201. Exposes `./Routes` (`src/app/app.routes.ts` -> `Mail` feature in `src/app/mail.ts`).
Three-pane Outlook-style mail UI. Imports `<lib-panel>` from `shared`. Zoneless; use signals.
Registered in the shell via `projects/shell/public/federation.manifest.json`.
